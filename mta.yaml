_schema-version: "3.2"
ID: travels
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travels-destination-content
  type: com.sap.application.content
  requires:
  - name: travels-destination-service
    parameters:
      content-target: true
  - name: travels-repo-host
    parameters:
      service-key:
        name: travels-repo-host-key
  - name: travels-uaa
    parameters:
      service-key:
        name: travels-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travels_html_repo_host
          ServiceInstanceName: travels-html5-srv
          ServiceKeyName: travels-repo-host-key
          sap.cloud.service: travels
        - Authentication: OAuth2UserTokenExchange
          Name: travels_uaa
          ServiceInstanceName: travels-xsuaa-srv
          ServiceKeyName: travels-uaa-key
          sap.cloud.service: travels
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: travels-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travels-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travels.zip
      name: travels
      target-path: resources/
- name: travels
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travels-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travels-destination-service
    service-plan: lite
- name: travels-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travels-xsuaa-srv
    service-plan: application
- name: travels-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travels-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
